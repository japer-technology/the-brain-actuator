{
  "name": "the-brain-actuator",
  "version": "0.0.0",
  "private": true,
  "description": "TheLocalBrain — a safety-first local data plane over TheBrain's on-disk files.",
  "license": "SEE LICENSE IN README.md",
  "type": "module",
  "workspaces": [
    "core",
    "surfaces/cli",
    "surfaces/mcp"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "npm run build && node --test \"core/dist/**/*.test.js\"",
    "lint": "tsc --build",
    "cli": "node surfaces/cli/dist/bin.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
